python -m venv .venv
.venv\Scripts\activate
deactivate

uvicorn main:app --reload

pip install -r .\requirements.txt
pip freeze > requirements.txt                               # atualizar os requirements.txt


# migrar o banco de dados (toda vez que algum model tiver 
os atributos (colunas) modificados/alterados/adicionados/deletados)

alembic revision --autogenerate -m "Nome da migração"      # criar a migração
alembic upgrade head                                       # executar a migração 
alembic history                                            # visualiza o histórico de migrações